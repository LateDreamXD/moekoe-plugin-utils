name: Publish

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    permissions:
      id-token: write  # Required for OIDC
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          cache: pnpm
          node-version: 24
      - name: update npm
        run: npm install -g npm@latest
      - name: install deps
        run: pnpm install --frozen-lockfile
      - name: build
        run: pnpm run build
      - name: test
        run: pnpm run test
      - name: publish
        if: ${{ !contains(github.ref, 'alpha') && !contains(github.ref, 'beta') && !contains(github.ref, 'rc') }}
        run: pnpm publish --no-git-checks --provenance
      - name: publish (alpha)
        if: ${{ contains(github.ref, 'alpha') }}
        run: pnpm publish --no-git-checks --provenance --tag alpha
      - name: publish (beta)
        if: ${{ contains(github.ref, 'beta') }}
        run: pnpm publish --no-git-checks --provenance --tag beta
      - name: publish (rc)
        if: ${{ contains(github.ref, 'rc') }}
        run: pnpm publish --no-git-checks --provenance --tag rc
